# solhint

Nix package for solhint, a Solidity linter.

## Files

- `default.nix` - Package definition
- `source.json` - Version and hashes
- `update.sh` - Update script

## Updating

Run from repository root:

```bash
./nix/solhint/update.sh
```

Updates `source.json` to latest release.
