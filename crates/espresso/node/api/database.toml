[route.table_sizes]
PATH = ["/database/table-sizes"]
METHOD = "GET"
DOC = """
Get the sizes of all database tables.

Returns a map from table name to size information, including row counts and disk usage.
Works with both PostgreSQL and SQLite backends (via embedded-db feature).
"""
